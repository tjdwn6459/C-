int main() //main함수이고 int로 출력받는다
{

	return 0; //그래서 함수끝엔 return 있아야한다
}

void sum(int a, int b) //두개의 int형의 변수(선언형태)를 입력받는다
{
	                   //함수가 실행되려면 호출해야한다 (main), 메인에서 사용하는 실제 인수를 적어준다(입력)
}